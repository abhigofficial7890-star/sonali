<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Happy Birthday Sonali 🎂</title>

<style>
body {
  margin: 0;
  background: linear-gradient(135deg, #ff9a9e, #fad0c4);
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  font-family: 'Comic Sans MS', cursive;
  overflow: hidden;
}

/* BOOK */
.book {
  width: 90%;
  max-width: 350px;
  height: 500px;
  position: relative;
  perspective: 1500px;
}

.page {
  width: 100%;
  height: 100%;
  position: absolute;
  background: white;
  border-radius: 15px;
  box-shadow: 0 10px 25px rgba(0,0,0,0.3);
  transform-origin: left;
  transition: transform 1s;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 20px;
  text-align: center;
  backface-visibility: hidden;
  overflow: hidden;
}

.page:nth-child(2) { background: #fff0f5; }
.page:nth-child(3) { background: #ffe4e1; }

.page.flipped {
  transform: rotateY(-180deg);
}

h1, h2, p {
  color: #ff4081;
  z-index: 2;
}

/* Falling Hearts */
.heart {
  position: absolute;
  color: red;
  font-size: 20px;
  animation: fall linear infinite;
  opacity: 0.8;
}

@keyframes fall {
  0% { transform: translateY(-10%); opacity: 1; }
  100% { transform: translateY(110%); opacity: 0; }
}

/* Sparkles */
.sparkle {
  position: absolute;
  width: 5px;
  height: 5px;
  background: gold;
  border-radius: 50%;
  animation: sparkle 1.5s infinite alternate;
}

@keyframes sparkle {
  from { opacity: 0.2; transform: scale(1); }
  to { opacity: 1; transform: scale(1.8); }
}

/* Cute Bubu Hug */
.bubu {
  position: relative;
  width: 150px;
  height: 150px;
}

.bear {
  width: 70px;
  height: 90px;
  background: #ffb6c1;
  border-radius: 50px;
  position: absolute;
  top: 30px;
  animation: hug 1.5s infinite alternate;
}

.bear.left { left: 0; }
.bear.right { right: 0; }

@keyframes hug {
  from { transform: translateX(0); }
  to { transform: translateX(10px); }
}

/* Typing cursor */
#typedMessage::after {
  content: "|";
  animation: blink 1s infinite;
}

@keyframes blink {
  50% { opacity: 0; }
}

/* Fireworks */
canvas {
  position: fixed;
  top: 0;
  left: 0;
  pointer-events: none;
}

</style>
</head>

<body onclick="nextPage()">

<canvas id="fireworks"></canvas>

<div class="book">
  <div class="page" id="page1">
    <h1>🎂 Happy Birthday<br>My Favourite Person 💖</h1>
  </div>

  <div class="page" id="page2">
    <div>
      <div class="bubu">
        <div class="bear left"></div>
        <div class="bear right"></div>
      </div>
      <h2>Cute Bubu Hug 🤗</h2>
    </div>
  </div>

  <div class="page" id="page3">
    <p id="typedMessage"></p>
  </div>
</div>

<script>
let currentPage = 1;
let typedStarted = false;

function nextPage() {
  if (currentPage <= 3) {
    document.getElementById("page" + currentPage)
      .classList.add("flipped");

    if (currentPage === 2 && !typedStarted) {
      setTimeout(startTyping, 1000);
      typedStarted = true;
    }

    currentPage++;
  }
}

/* Typing Effect */
const message = `🌸 Dear Sonali 🌸

May your birthday be filled with love, laughter,
happiness and endless smiles 💕

You deserve all the joy in the world.
Stay blessed, stay beautiful and keep shining ✨

Happy Birthday once again! 🎉🎂`;

let index = 0;

function startTyping() {
  function type() {
    if (index < message.length) {
      document.getElementById("typedMessage").innerHTML +=
        message.charAt(index) === "\n" ? "<br>" : message.charAt(index);
      index++;
      setTimeout(type, 40);
    }
  }
  type();
}

/* Falling Hearts */
function createHeart() {
  const heart = document.createElement("div");
  heart.classList.add("heart");
  heart.innerHTML = "❤";
  heart.style.left = Math.random() * 100 + "%";
  heart.style.animationDuration = (Math.random() * 3 + 2) + "s";
  document.body.appendChild(heart);
  setTimeout(() => heart.remove(), 5000);
}
setInterval(createHeart, 300);

/* Sparkles */
function createSparkle() {
  const sparkle = document.createElement("div");
  sparkle.classList.add("sparkle");
  sparkle.style.top = Math.random() * 100 + "%";
  sparkle.style.left = Math.random() * 100 + "%";
  document.body.appendChild(sparkle);
  setTimeout(() => sparkle.remove(), 1500);
}
setInterval(createSparkle, 200);

/* Simple Fireworks */
const canvas = document.getElementById("fireworks");
const ctx = canvas.getContext("2d");
canvas.width = window.innerWidth;
canvas.height = window.innerHeight;

function firework() {
  let x = Math.random() * canvas.width;
  let y = Math.random() * canvas.height / 2;

  for (let i = 0; i < 30; i++) {
    ctx.beginPath();
    ctx.arc(x, y, 2, 0, Math.PI * 2);
    ctx.fillStyle = "hsl(" + Math.random() * 360 + ",100%,50%)";
    ctx.fill();
  }
}
setInterval(firework, 800);

</script>

</body>
</html>
